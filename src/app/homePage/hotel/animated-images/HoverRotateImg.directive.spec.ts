/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { HoverRotateImgDirective } from './HoverRotateImg.directive';

describe('Directive: HoverRotateImg', () => {
  it('should create an instance', () => {
    const directive = new HoverRotateImgDirective();
    expect(directive).toBeTruthy();
  });
});
